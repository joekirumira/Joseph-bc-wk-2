
from Dojo import create_room
from Office import Office
from Living_space import LivingSpace
form Dojo import add_person



class TestCreateRoom(unittest.TestCase):
	def create_room(room_type, room_name):
		room_type.AssertIsEqual(room_type("room_type"), "str")
		room_name.AssertIsEqual(room_name("room_name"), "str")


	def room_type(self):
		  self.room_type == room_type
		if room_type == Office:
		   return "Office"
		else if room_type == Living_space:
		   return "Living_space"
		else
		   return "Allocate room_type"
	
	def room_name(self):
		  self.room_name == room_name
		    return "room_name"

    def maximum_num_rooms(self):
    	  self.maximum_num_rooms = maximum_num_rooms
        if maximum_num_rooms > 99:
        	return Error "Dojo is at full capacity"
        else:
             return "A room has been created"


class TestAddPerson(unittest.TestCase):
	def add_person(person_name, person_type, wants_accomodation)
	   




		

